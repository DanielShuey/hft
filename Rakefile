root = File.expand_path('..', __FILE__)

# Gems
require 'rubygems'
require 'bundler'
Bundler.require(:default)

# App
Dir[File.join(root, "/app/**/*.rb")].each do |file|
  require file
end

task :update_data do

end
